using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class audioyeet : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Resources.Load<GameObject>("SoundManager");
    }
}
